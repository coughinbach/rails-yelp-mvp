<%= simple_form_for [@restaurant, @review] do |f| %>

    <div class="form-group">
      <%= f.input :content %>
    </div>
    <div class="form-group">
      <%= f.input :rating %>
    </div>
    <%= f.submit class: 'btn btn-primary' %>
<% end %>
